version: v1
kind: ServiceTemplate
metadata:
  name: dotnet-backend-agent
  description: .NET backend agent created by meshagent create.
variables:
  - name: image
    title: Container image
    description: Image tag built and deployed by meshagent deploy.
    optional: true
container:
  image: "{{ image }}"
  template: agent
  private: false
