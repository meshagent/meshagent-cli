version: v1
kind: ServiceTemplate
metadata:
  name: python-webserver
  description: Python web app created by meshagent create.
variables:
  - name: image
    title: Container image
    description: Image tag built and deployed by meshagent deploy.
    optional: true
  - name: domain
    title: Public domain
    description: Public route domain for the deployed web service.
    type: route
    optional: true
container:
  image: meshagent/python:default
  template: none
  command: python -m server
  working_dir: /app
  storage:
    images:
      - image: "{{ image }}"
        path: /app
        subpath: app
        read_only: true
  private: false
ports:
  - num: 8000
    type: http
    published: true
    public: true
    liveness: /health
