[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "meshagent-create-python-contact-form"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "aiohttp~=3.13.0",
]

[tool.setuptools]
py-modules = ["server"]
